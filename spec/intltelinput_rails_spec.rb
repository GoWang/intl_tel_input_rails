require 'spec_helper'

describe IntltelinputRails do
  it 'has a version number' do
    expect(IntltelinputRails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
